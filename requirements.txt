# The MCP protocol itself is implemented by hand on top of the Python
# standard library (no FastMCP, no MCP SDK), so the custom server has no
# third-party dependencies.

# Client for the LLM API used by the chatbot host (requirements 1-2).
# It is the HTTP client of the model, not an MCP implementation.
google-genai>=1.0.0

# Official Git MCP server from Anthropic (requirement 4). The host
# launches it as a separate process; the project code does not import it.
mcp-server-git