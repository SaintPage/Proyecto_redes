# El servidor MCP no tiene dependencias externas: el protocolo esta
# implementado a mano sobre la libreria estandar de Python.
#
# El chatbot anfitrion si necesita el SDK oficial de Google para
# hablar con la API del LLM. Ese SDK es el cliente HTTP del modelo, no
# una implementacion de MCP, por lo que su uso no contradice la
# restriccion del enunciado.
google-genai>=1.0.0
