# The MCP server itself uses only the Python standard library, as the
# protocol is implemented manually (no FastMCP, no MCP SDK).
# Dependencies for the chatbot host will be added in the next stage.
